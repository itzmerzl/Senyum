# Database Connection
DATABASE_URL="mysql://user:password@localhost:3306/senyum"

# Authentication (REQUIRED - Use strong random string min 32 chars)
# Generate new one with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your-secret-key-here-minimum-32-characters-long

# Server Configuration
PORT=3001
NODE_ENV=development

# IMPORTANT SECURITY NOTES:
# 1. NEVER commit .env file to Git
# 2. JWT_SECRET must be at least 32 characters
# 3. Use different JWT_SECRET for production
# 4. Keep this file secure and backed up
